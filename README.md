# buffered-line-reader

파일 디스크립터에서 입력을 한 줄씩 읽는 C99 정적 라이브러리를 구현한다.

## 초기 규약

- 공개 API와 내부 상태의 소유권을 구분한다.
- `BUFFER_SIZE`는 컴파일 시 지정하는 양의 정수로 취급한다.
- 시스템 호출, 할당, EOF의 경계를 각각 검증한다.
- 각 개발 단계는 경고 없는 컴파일과 해당 시점의 검증을 통과해야 한다.
- 빌드 산출물과 로컬 검사 결과는 버전 관리에 포함하지 않는다.

상세한 사용법과 최종 구조는 구현이 완료된 뒤 정리한다.
